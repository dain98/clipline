# Nightly 0.1.38 Release

> **For agentic workers:** Execute this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for
> tracking and remain unticked by repository convention.

**Goal:** Publish PR #100's recorder, elevation, export, fullscreen, and RAM-meter improvements as
the signed-updater Nightly 0.1.38 release.

## Task 1: Merge and release-version the approved change

- [ ] Confirm PR #100 targets `develop`, is mergeable, and has green Ubuntu, Windows, and review
      checks.
- [ ] Merge PR #100 with a merge commit and delete its feature branch.
- [ ] Advance the Cargo package, lockfile, and Tauri versions from 0.1.37 to 0.1.38.
- [ ] Add the Nightly 0.1.38 checkpoint to `handoff.md`.
- [ ] Commit and push the release version on `develop`.

## Task 2: Verify release inputs and quality gates

- [ ] Review Microsoft's current WebView2 Fixed Version release and confirm the pinned x64 runtime
      remains current.
- [ ] Run `verify-webview2-runtime.ps1 -RequirePayload`.
- [ ] Download and validate the exact hash-pinned LGPL-shared FFmpeg archive with
      `stage-ffmpeg-resource.ps1`.
- [ ] Run workspace tests and fresh-cache warning-denied workspace Clippy.
- [ ] Confirm the release commit's GitHub CI and security checks pass.

## Task 3: Build and publish both updater variants

- [ ] Load the local Tauri updater signing key without printing it.
- [ ] Build the regular NSIS installer and preserve its updater signature.
- [ ] Build the standalone NSIS installer with the reviewed fixed WebView2 runtime, rename it with
      the standalone suffix, and preserve its distinct updater signature.
- [ ] Author `latest.json`, `latest-standalone.json`, and release notes with exact asset URLs,
      version, publication time, signatures, source commit, and verification summary.
- [ ] Replace the moving `nightly` prerelease and tag only after every asset is staged locally.
- [ ] Verify the release target, all seven assets, updater metadata, signatures, hashes, and public
      download URLs.

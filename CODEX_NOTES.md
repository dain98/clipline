# Codex Notes

Follow-up ideas from 2026-06-12:

1. Done: record microphone audio in addition to system audio.
2. Done: Settings > Capture has audio input/output selection, input/output volume controls, a Mono checkbox, and a live Test mic monitor.
3. Add an encoder selection setting so users can choose the encoder that best fits their hardware and performance goals.
4. Done: sliders and scrollbars use custom Clipline styling instead of browser defaults.
5. In the Library, show an icon or badge for clips created by the user, including hotkey-saved clips and trimmed exports.
6. Add a top-left memory indicator, for example `Using 222 MB RAM`.

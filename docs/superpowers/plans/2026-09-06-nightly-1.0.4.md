# Nightly 1.0.4 Release

**Goal:** Publish the merged Library group lifecycle fixes, module decomposition, and Pink/Light
favorite colors from CI-green develop merge `17a514fd`.

- [ ] Confirm PRs #191, #193, and #194 are merged with green Windows/Ubuntu CI.
- [ ] Read rolling Nightly metadata (1.0.3) and advance Cargo, Cargo.lock, and Tauri to 1.0.4.
- [ ] Refresh the Microsoft Fixed Runtime pin to the live x64 selector's 152.0.4191.62; verify
      the CAB size/hash, update both standalone paths, and review through 2026-10-06.
- [ ] Stage the verified standalone runtime and smoke-test H.264/Opus playback and codec gating.
- [ ] Run WebView2 preflight, workspace tests, and warning-denied Clippy. Confirm the release diff
      from 17a514fd contains only version/runtime metadata and documentation.
- [ ] Commit and push release inputs to develop, then push immutable nightly-v1.0.4 at that commit.
- [ ] Watch Nightly Release through public-download/signature verification; confirm seven assets
      and the rolling release's exact commit. Never build/upload release assets manually.
- [ ] Record version, commit, release URL, and verification in handoff.md.

Runtime review sources: https://developer.microsoft.com/en-us/microsoft-edge/webview2/ and
https://learn.microsoft.com/en-us/microsoft-edge/webview2/release-notes/runtime/152 . The current
selector offers the servicing revision .62; the major-version release notes describe .53.

Preflight: verified the 371,877,369-byte CAB and SHA-256
`2dc7d817ddce4d036f33684425c218f1ca2d073911136e5d3f8ce9b2340569b5`; runtime executable
version and Microsoft Authenticode signature match. Workspace tests and fresh-app-cache Clippy
passed. A debug build using the complete standalone config ran the staged .62 runtime and played
H.264/Opus (90 frames / 3 seconds), HEVC (30 / 1), and AV1 (30 / 1) through `ended` without errors.
The runtime probes enabled those same three codecs. Desktop automation was unavailable; verification
used an ephemeral localhost WebView debugger connection, closed afterward. Test clips were removed.

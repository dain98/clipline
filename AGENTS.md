# Agent Notes

- Follow the project source of truth in `ddoc.md` and the current development state in
  `handoff.md`.
- After each implementation, open the Clipline app for the user to test.
- If an existing `clipline-app.exe` process is running and blocks the build or launch, stop it
  before rebuilding or reopening the app.
- After opening the app, give the user a concise list of specific things to test for the change
  that was just implemented.
